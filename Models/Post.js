//const mongoose = require('mongoose');
//const Schema = mongoose.Schema;

//Scheme for Zeta Initiate Posts
//Ability to share
//tag system
//images show up on small bubbles at the bottom of post. click to expand
/* const PostSchema = new Schema({
    user:{
        type: Schema.Types.ObjectId,
        ref: 'admin'
    },
    title:{
        type: String
    },
    images:{
        type: String,
    },
    text:{
        type: String,
        required: true
    },
    date: {
        type: Date,
        default: Date.now
    }
});

module.exports = Post = mongoose.model('post', PostSchema); */